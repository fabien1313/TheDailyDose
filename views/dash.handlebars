<main>
    {{#each posts}}
    <div class="post">
        <h2 class="post-title">{{title}}</h2>
        <p class="content">{{post}}</p>
    </div>
    {{/each}}
</main>

<div id="create-btn-div">
    <a href=""><button id="create-btn">Create Blog Post</button></a>
</div>